# Copy to .env and fill in. .env is gitignored.

# Which model to use. A claude-* name uses Claude (needs ANTHROPIC_API_KEY); a
# gemini-* name uses Gemini (needs GEMINI_API_KEY or GOOGLE_API_KEY). Defaults
# to claude-opus-4-8 if unset.
# AGENTBOX_MODEL=claude-opus-4-8
# AGENTBOX_MODEL=gemini-2.5-pro

# Set the key for whichever model you chose.
ANTHROPIC_API_KEY=sk-ant-...
# GEMINI_API_KEY=...

# Optional: read-only email access (IMAP). Set all three to enable the email
# tools. Use an app password, not your account password.
# AGENTBOX_IMAP_HOST=imap.gmail.com
# AGENTBOX_IMAP_PORT=993
# AGENTBOX_IMAP_USER=you@example.com
# AGENTBOX_IMAP_PASS=your-app-password
# Default lookback window for email tools, in days (0/unset = no date limit):
# AGENTBOX_EMAIL_SINCE_DAYS=7

# Optional: read-only calendar via iCal (ICS) feed URLs, comma-separated.
# In Google Calendar: Settings > [calendar] > Integrate calendar >
# "Secret address in iCal format". AGENTBOX_TIMEZONE sets day boundaries.
# AGENTBOX_ICS_URLS=https://calendar.google.com/calendar/ical/.../basic.ics
# AGENTBOX_TIMEZONE=America/New_York

# Verbose debug logging to stderr (model/tool I/O, memory, capture decisions):
# AGENTBOX_DEBUG=1

# Daily-output journal: the assistant appends each scheduled task's result to
# a dated markdown file (journal/YYYY-MM-DD.md). Override the location if you like:
# AGENTBOX_JOURNAL_DIR=journal
